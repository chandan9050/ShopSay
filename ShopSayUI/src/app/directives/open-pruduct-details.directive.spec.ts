import { OpenPruductDetailsDirective } from './open-pruduct-details.directive';

describe('OpenPruductDetailsDirective', () => {
  it('should create an instance', () => {
    const directive = new OpenPruductDetailsDirective();
    expect(directive).toBeTruthy();
  });
});
